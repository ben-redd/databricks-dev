fileVersion: 1
id: "125"
mappingDefinitions:
  DESTINATION:
    database: ben-dev
    schema: dev-token-auth-env
  SOURCE:
    database: coalesce
    schema: sample_data
name: DBX token auth env
type: Environment
